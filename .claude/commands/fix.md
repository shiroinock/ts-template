# fix

ビルド、lint、テストを実行して、エラーがあれば修正します。

## 手順

1. `pnpm lint` を実行してlintエラーを確認
2. エラーがあれば `pnpm lint:fix` で自動修正を試みる
3. 自動修正できないエラーは手動で修正
4. `pnpm typecheck` を実行して型エラーを確認
5. 型エラーがあれば修正
6. `pnpm test` を実行してテストを確認
7. テストが失敗していれば修正
8. `pnpm build` を実行してビルドを確認
